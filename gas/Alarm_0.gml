// Set the lifetime of the object in steps
lifetime = 300; // This object will live for 300 steps (you can adjust this)

// Start the alarm to track the lifetime
alarm[0] = lifetime;
// This is executed when the lifetime of the object expires
instance_destroy(); // Destroy the object










